import { View, Text } from "react-native";
import React from "react";
import { ModalScreen } from "../../presentation/screens/layout/ModalScreen";

const Modal = () => {
  return <ModalScreen />;
};

export default Modal;
