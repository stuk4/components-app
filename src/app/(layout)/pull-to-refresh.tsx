import { View, Text } from "react-native";
import React from "react";
import { PullToRefreshScreen } from "../../presentation/screens/layout/PullToRefreshScreen";

const PullToRefresh = () => {
  return <PullToRefreshScreen />;
};

export default PullToRefresh;
