import { View, Text } from "react-native";
import React from "react";
import { TextInputsScreen } from "../../presentation/screens/inputs/TextInputsScreen";

const TextInputs = () => {
  return <TextInputsScreen />;
};

export default TextInputs;
